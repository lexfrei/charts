Eclipse EDC Connector has been deployed!

Participant ID: {{ .Values.connector.config.participantId }}

Service endpoints:
{{- $fullName := include "eclipse-edc.fullname" . }}
  - Default API:    http://{{ $fullName }}:{{ .Values.connector.ports.default }}/api
  - Management API: http://{{ $fullName }}:{{ .Values.connector.ports.management }}/management
  - Protocol (DSP): http://{{ $fullName }}:{{ .Values.connector.ports.protocol }}/protocol
  - Public API:     http://{{ $fullName }}:{{ .Values.connector.ports.public }}/public
  - Control API:    http://{{ $fullName }}:{{ .Values.connector.ports.control }}/control

{{- if .Values.connector.config.hostname }}

External access (via hostname {{ .Values.connector.config.hostname }}):
  - DSP Callback:   {{ include "eclipse-edc.dspCallbackAddress" . }}
  - Public API:     {{ include "eclipse-edc.publicBaseUrl" . }}
{{- end }}

{{- if .Values.ingress.enabled }}

Ingress is enabled. Make sure your ingress controller is configured.
{{- end }}

To check the connector status:
  kubectl get pods -l "app.kubernetes.io/name={{ include "eclipse-edc.name" . }},app.kubernetes.io/instance={{ .Release.Name }}"

To view logs:
  kubectl logs -l "app.kubernetes.io/name={{ include "eclipse-edc.name" . }},app.kubernetes.io/instance={{ .Release.Name }}"

NOTE: This connector uses InMemoryVault which is NOT suitable for production.
For production, configure HashiCorp Vault or another secure vault solution.
